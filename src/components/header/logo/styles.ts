import styled from "styled-components";

export const Container = styled.nav`
  cursor: pointer;
  p {
    color: var(--white);
    font-weight: 600;
    font-size: 1.5rem;
  }
  b {
    color: var(--blue);
  }
`;
